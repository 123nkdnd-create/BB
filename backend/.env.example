# Backend environment variables
PORT=5000
ATLAS_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
JWT_SECRET=replace_me_with_a_long_random_string
# Optional: provide this to allow self-service admin registration via adminCode
ADMIN_CODE=your_admin_invite_code
# Alternative approach: seed a default admin user via these
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=supersecret